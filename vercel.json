{
  "builds": [
    { "src": "vite.config.ts", "use": "@vercel/static-build" },
    { "src": "backend/**/*.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "backend/$1.ts" },
    { "src": "/(.*)", "dest": "/" }
  ]
}